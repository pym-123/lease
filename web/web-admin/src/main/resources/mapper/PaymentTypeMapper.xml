<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.lease.web.admin.mapper.PaymentTypeMapper">

    <select id="selectPaymentTypeList" resultType="com.atguigu.lease.model.entity.PaymentType">
        select pt.* from payment_type pt left join room_payment_type rpt on pt.id = rpt.payment_type_id and rpt.is_deleted = 0  where rpt.room_id = #{id} and pt.is_deleted = 0
    </select>
</mapper>
