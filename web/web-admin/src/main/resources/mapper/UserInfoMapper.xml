<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.lease.web.admin.mapper.UserInfoMapper">

    <select id="selectpageUserInfo" resultType="com.atguigu.lease.model.entity.UserInfo">
        select * from user_info
        <where>
            <if test="queryVo.phone!=null and queryVo.phone!=''">
                phone like "%"#{queryVo.phone}"%"
            </if>
            <if test="queryVo.status!=null">
                and status = #{queryVo.status}
            </if>
        </where>
    </select>
</mapper>
