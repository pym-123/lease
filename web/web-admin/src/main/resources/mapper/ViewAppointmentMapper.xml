<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.lease.web.admin.mapper.ViewAppointmentMapper">

    <resultMap id="selectPageAppointmentVoListResultMap" type="com.atguigu.lease.web.admin.vo.appointment.AppointmentVo" autoMapping="true">
        <id column="va_id" property="id"/>
        <result column="va_name" property="name"/>
        <result column="va_phone" property="phone"/>
        <association property="apartmentInfo" javaType="com.atguigu.lease.model.entity.ApartmentInfo" autoMapping="true">
            <result column="ai_id" property="id"/>
            <result column="ai_name" property="name"/>
            <result column="ai_phone" property="phone"/>
        </association>
    </resultMap>

    <select id="selectPageAppointmentVoList" resultMap="selectPageAppointmentVoListResultMap">
        SELECT
            va.id as va_id,
            va.user_id,
            va.name as va_name,
            va.phone as va_phone,
            va.apartment_id,
            va.appointment_time,
            va.additional_info ,
            va.appointment_status,
            ai.id ai_id,
            ai.name ai_name,
            ai.introduction,
            ai.district_id,
            ai.district_name,
            ai.city_id,
            ai.city_name,
            ai.province_id,
            ai.province_name,
            ai.address_detail,
            ai.latitude,
            ai.longitude,
            ai.phone ai_phone,
            ai.is_release
        FROM
            view_appointment va,
            apartment_info ai
        <where>
            va.apartment_id = ai.id
            AND va.is_deleted = 0
            AND ai.is_deleted = 0
            <if test="queryVo.provinceId!=null">
                and ai.province_id = #{queryVo.provinceId}
            </if>
            <if test="queryVo.cityId!=null">
                and ai.city_id = #{queryVo.cityId}
            </if>
            <if test="queryVo.districtId!=null">
                and ai.district_id = #{queryVo.districtId}
            </if>
            <if test="queryVo.apartmentId!=null">
                and ai.id = #{queryVo.apartmentId}
            </if>
            <if test="queryVo.name!=null and queryVo.name!=''">
                and va.name like "%"#{queryVo.name}"%"
            </if>
            <if test="queryVo.phone!=null and queryVo.phone!=''">
                and va.phone like "%"#{queryVo.phone}"%"
            </if>
        </where>


    </select>
</mapper>
